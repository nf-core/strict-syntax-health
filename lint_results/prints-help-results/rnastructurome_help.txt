$ NXF_SYNTAX_PARSER=v2 nextflow run . --help


 N E X T F L O W   ~  version 26.07.0-edge

Launching `./main.nf` [tender_curry] revision: 5201ee498b


------------------------------------------------------
                                        ,--./,-.
        ___     __   __   __   ___     /,-._.--~'
  |\ | |__  __ /  ` /  \ |__) |__         }  {
  | \| |       \__, \__/ |  \ |___     \`-._,-`-,
                                        `._,._,'
  nf-core/rnastructurome 1.0.0
------------------------------------------------------
Typical pipeline command:

  nextflow run nf-core/rnastructurome -profile <docker/singularity/.../institute> --input samplesheet.csv --outdir <OUTDIR>

help message of that parameter will be printed.  
or `--helpFull`.  

Input/output options
  --input                              [string] Path to comma-separated file containing information about the samples in the experiment. 
  --outdir                             [string] The output directory where the results will be saved. You have to use absolute paths to storage on 
Cloud infrastructure.  
  --email                              [string] Email address for completion summary. 
  --multiqc_title                      [string] MultiQC report title. Printed as page header, used for filename if not otherwise specified. 

Reference genome options
  --fasta                              [string] Path to transcript FASTA file. 
  --gtf                                [string] Path to transcript annotation GTF file. 
  --ensembl_base_url                   [string] Ensembl FTP base URL used when auto-resolving transcript FASTA. [default: 
https://ftp.ensembl.org/pub]  
  --ensembl_release                    [string] Ensembl release for transcript FASTA auto-resolution (`current`, `latest`, or release number like 
`114`). [default: current]  

Sample metadata options
  --sample_id                          [string]  Fallback sample ID for rows missing `sample_id`. 
  --method                             [string]  Fallback probing method for rows missing `method`. 
  --principle                          [string]  Fallback probing principle for rows missing `principle`.  (accepted: RT-stop, MaP, rt-stop, map) 
 
  --organism                           [string]  Fallback organism for rows missing `organism`, typically a Latin binomial such as `Homo sapiens`. 
 
  --chemical                           [string]  Fallback chemical probing reagent for rows missing `chemical` (e.g. `1M7`). 
  --RT_enzyme                          [string]  Fallback reverse transcriptase enzyme for rows missing `RT_enzyme` (e.g. `M-MLV`). 
  --pH                                 [number]  Fallback DMS reaction pH for rows missing `pH`. 
  --umi_pattern                        [string]  Fallback UMI pattern for rows missing `umi_pattern`. Supplying a pattern enables umi_tools extract 
before cutadapt.  
  --fuzzy_untreated_pairing            [boolean] When true (default), a treated group with no exact `sample_group`+`replicate` untreated match falls 
back to the untreated sample sharing the same sample_group base token (portion before the first `_`) 
at the same replicate — e.g. `MDA-MB-231_untreated_r1` can serve as control for 
`MDA-MB-231_MTX_treated_r1`. If a treated group still has no untreated after that, and exactly one 
untreated control exists anywhere else in the same reference, that single control is reused for it 
(with a warning) — e.g. one shared untreated backing several treated replicates. Set false to 
require exact matches; unmatched treated groups then proceed without an untreated control (scoring 
method 2 or 4). [default: true]  

Read trimming options
  --cutadapt_adapter_5p                [string]  Global fallback 5' adapter sequence(s), comma-separated. If omitted and no per-sample adapter is 
provided, the pipeline defaults to AGATCGGAAGAGC. [default: AGATCGGAAGAGC]  
  --cutadapt_adapter_3p                [string]  Global fallback 3' adapter sequence(s), comma-separated. If omitted and no per-sample adapter is 
provided, the pipeline defaults to AGATCGGAAGAGC. [default: AGATCGGAAGAGC]  
  --cutadapt_quality_only              [boolean] If true, skip adapter trimming and apply quality/length trimming only. [default: false] 
  --cutadapt_5quality                  [integer] Base quality threshold for 5' trimming (used for MaP; RT-stop is forced to 0). [default: 20] 
 
  --cutadapt_3quality                  [integer] Base quality threshold for 3' trimming. [default: 20] 
  --cutadapt_len                       [integer] Minimum read length retained after trimming. [default: 25] 
  --cutadapt_min_align                 [integer] Minimum adapter overlap required for trimming. [default: 3] 
  --cutadapt_trim_n                    [boolean] Trim terminal N bases. [default: true] 

Alignment options
  --transcriptome                      [boolean]         Align against a transcript-level FASTA using Bowtie (RT-stop) / Bowtie2 (MaP). Default (false) 
downloads a genome FASTA and aligns with STAR. [default: false]  
  --count_genome                       [boolean]         Genome route (STAR) quantification method. Default (false) uses STAR --quantMode TranscriptomeSAM 
projected onto transcripts, tag-corrected with samtools calmd, counted with rf-count. Set true for 
the legacy rf-count-genome + rf-rctools extract path. [default: false]  
  --star_multimap_nmax                 [integer]         Maximum number of loci a read is allowed to map to in STAR (`--outFilterMultimapNmax`). Reads 
mapping to more loci than this are discarded. Increase when studying repetitive non-coding RNAs. 
[default: 10]  
  --bowtie_k                           [integer]         Report up to this number of alignments per read (`-k`). [default: 1] 
  --bowtie_all                         [boolean, string] Report all valid alignments (`-a`). [default: true] 
  --bowtie_trim5                       [integer]         Trim this many bases from 5' read end (`--trim5`). [default: 0] 
  --bowtie_trim3                       [integer]         Trim this many bases from 3' read end (`--trim3`). [default: 0] 
  --bowtie_index                       [string]          Optional path to a prebuilt Bowtie index (currently not consumed by workflow logic). 
  --bowtie_n                           [integer]         Bowtie v1 `-n` seed mismatch mode value. [default: 2] 
  --bowtie_v                           [integer]         Bowtie v1 `-v` mismatch mode value. 
  --bowtie_max                         [integer]         Bowtie v1 `-m` maximum reportable alignments per read before suppression. [default: 1] 
  --bowtie_chunkmbs                    [integer]         Bowtie v1 `--chunkmbs` memory chunk size. [default: 512] 
  --bowtie2_preset                     [string]          Bowtie2 alignment preset. Default is equivalent to `--local -N 0 -D 20 -R 3 -L 20 -i S,1,0.50`. 
[default: --very-sensitive-local]  
  --bowtie2_mp                         [string]          Bowtie2 `--mp` mismatch penalties as `max,min`. [default: 6,2] 
  --bowtie2_dpad                       [integer]         Bowtie2 `--dpad` padding around DP table. [default: 15] 
  --bowtie2_rdg                        [string]          Bowtie2 `--rdg` read gap penalties as `open,extend`. [default: 5,3] 
  --bowtie2_rfg                        [string]          Bowtie2 `--rfg` reference gap penalties as `open,extend`. [default: 5,3] 
  --bowtie2_softclip                   [boolean]         Enable Bowtie2 local alignment mode (`--local`). 
  --bowtie2_ma                         [integer]         Bowtie2 `--ma` match bonus used with `--local`. [default: 2] 
  --bowtie2_dovetail                   [boolean]         Allow dovetailing mates (`--dovetail`). [default: true] 
  --skip_markdup                       [boolean]         Skip SAMtools markdup deduplication (default: true). Position-based duplicate removal is not valid 
for chemical-probing libraries without UMIs, where reads sharing a 5' start are independent RT 
events rather than PCR duplicates. Leave enabled; UMI libraries are still deduplicated via 
UMI-tools. Set false only if you have a specific reason. [default: true]  
  --star_map_sjdb_overhang             [integer]         STAR `--sjdbOverhang` for MaP alignment. Ideally `readLength - 1`; default 200 suits 201 bp reads. 
[default: 200]  

RNAframework options
  --rfcount_img                        [boolean]          Deprecated: rf-count plot generation (`-g`) is always enabled. [default: false] 
  --rfcount_strandedness               [string]           Library strandedness fallback for rf-count-genome when RSeQC inference is unavailable (e.g. 
viral/bacterial references without a usable BED annotation).  (accepted: unstranded, first, 
second) [default: unstranded]  
  --rfcount_genome_map_quality         [integer]          Minimum MAPQ score for rf-count-genome (`--map-quality`). Useful for filtering multi-mappers; 0 
keeps all alignments. [default: 0]  
  --rfcount_genome_block_size          [integer]          Block size for per-chromosome memory allocation in rf-count-genome (`-bs`); increase for large 
genomes. [default: 100000]  
  --rfcount_genome_samtools_threads    [integer]          SAMtools working threads per processor instance in rf-count-genome (`-wt`); total threads = `-p` × 
`-wt`. [default: 1]  
  --rfcount_map_median_quality         [integer]          MaP only: discard reads with median Phred+33 base quality below this value (`-eq`). [default: 
20]  
  --rfcount_map_no_deletions           [boolean]          MaP only: ignore all deletion events during mutation counting (`-nd`). [default: false] 
  --rfcount_map_no_ambiguous           [boolean]          MaP only: ignore ambiguously aligned deletion events (`-na`). Auto: off for M-MLV, on for Group II 
Intron (e.g. TGIRT); leave unset to use the auto default.  
  --rfcount_map_no_insertions          [boolean]          MaP only: ignore insertion events during mutation counting (`-ni`). Auto: off for M-MLV, on for 
Group II Intron (e.g. TGIRT); leave unset to use the auto default.  
  --rfcount_map_right_deletion         [boolean]          MaP only: mark only the right-most base of a deletion as mutated (`--right-deletion`). Auto: on for 
M-MLV, off for Group II Intron (e.g. TGIRT); leave unset to use the auto default.  
  --rfcount_map_discard_consecutive    [integer]          MaP only: discard mutations within N nt of each other (`-dc N`). Auto: 3 when `RT_enzyme` is a Group 
II Intron RT (e.g. TGIRT), off for M-MLV; leave unset to use the auto default.  
  --rfcount_map_eval_surrounding       [boolean]          MaP only: also evaluate quality of ±1 nt surrounding each mutation (`-es`). [default: true] 
  --rfcount_trim_5prime                [integer]          Trim this many bases from the 5' read end for rf-count (`-t5`). [default: 0] 
  --rfcount_mask_file                  [string]           Path to rf-count mask file (`--mask-file`). 
  --rfcount_primary_only               [boolean]          Use only primary alignments in rf-count (`--primary-only`). [default: false] 
  --rfcount_discard_clipped_reads      [boolean]          Discard reads with excessive 5' or 3' end clipping in rf-count / rf-count-genome 
(`--discard-clipped-reads`). [default: false]  
  --rfcount_paired_only                [boolean]          For paired-end samples, use only reads with both mates mapped (`--paired-only`). [default: 
false]  
  --rfcount_properly_paired            [boolean]          For paired-end samples, use only properly paired reads (`--properly-paired`). [default: false] 
 
  --rfcount_map_sort_by_read_name      [boolean]          In mutation mode (`-m`), pre-sort paired-end reads by read name (`--sort-by-read-name`). No effect 
on single-end data. [default: true]  
  --rfcount_map_discard_shorter        [integer]          In mutation mode (`-m`), discard reads shorter than this value (`--discard-shorter`). [default: 
1]  
  --rfcount_map_min_quality            [integer]          In mutation mode (`-m`), minimum Phred+33 base quality for mutations (`--min-quality`). 
[default: 20]  
  --rfcount_map_collapse_consecutive   [boolean]          In mutation mode (`-m`), collapse consecutive mutations/indels (`--collapse-consecutive`). Auto: on 
unless `RT_enzyme` is a Group II Intron RT (e.g. TGIRT); leave unset to use the auto default. 
 
  --rfcount_map_max_collapse_distance  [integer]          In mutation mode with collapsing enabled, max distance to collapse (`--max-collapse-distance`). 
[default: 2]  
  --rfnorm_remap_reactivities          [boolean]          Remap normalized reactivities to the 0-1 range according to Zarringhalam et al. 
(`--remap-reactivities`). [default: false]  
  --rfnorm_reactive_bases              [string]           Reactive bases used for normalization window selection (`--reactive-bases`), e.g. `AC` for DMS. 
 
  --rfnorm_norm_window                 [integer]          Normalization window size (`--norm-window`). Auto: 50 for DMS or RT-stop, unset otherwise; leave 
unset to use the auto default.  
  --rfnorm_window_offset               [integer]          Normalization window offset (`--window-offset`). 
  --rfnorm_dynamic_window              [integer, boolean] Enable dynamic normalization window (`--dynamic-window`). Set to a positive integer to enable (value 
is passed to `--norm-window`); set to `false` or `0` to explicitly disable even for DMS (which 
enables it by default). Leave unset to use the per-method default.  
  --rfnorm_norm_independent            [boolean]          Normalize each reactive base independently (`--norm-independent`). [default: false] 
  --rfnorm_use_normfactor              [boolean]          Cross-experiment normalization via rf-normfactor (one factor set per reference, fed to rf-norm via 
`-nf`), putting reactivities on a common scale. Leave unset for auto (enabled only for a reference 
with more than one treated sample to cross-normalise); set `true` to force on, `false` to force off 
(per-sample box-plot).  
  --rfnorm_normfactor_min_coverage     [integer]          rf-normfactor minimum coverage (`-mc`): bases below this coverage are excluded from the 
normalization factor calculation. Auto: 1000 for MaP, 50 for RT-stop; leave unset to use the auto 
default.  
  --rfnorm_score_method                [integer]          Override rf-norm scoring method (`-sm`). 1=Ding, 2=Rouskin, 3=Siegfried, 4=Zubradt.  (accepted: 
1, 2, 3, 4)  
  --rfnorm_norm_method                 [integer]          Override rf-norm normalization method (`-nm`). 2=90% Winsorizing, 3=Box-plot, 4=Mitchell (MaP only). 
 (accepted: 2, 3, 4)  
  --rfnorm_raw                         [boolean]          Score raw reactivities without applying normalization (`--raw`). [default: false] 
  --rfnorm_pseudocount                 [number]           Pseudocount used by Ding scoring (`--pseudocount`). 
  --rfnorm_ignore_lower_than_untreated [boolean]          Set reactivities lower than untreated to zero for Ding or Siegfried scoring 
(`--ignore-lower-than-untreated`). [default: false]  
  --rfnorm_mean_coverage               [number]           Discard transcripts with mean coverage below this threshold (`--mean-coverage`). [default: 0] 
 
  --rfnorm_median_coverage             [number]           Discard transcripts with median coverage below this threshold (`--median-coverage`). [default: 
0]  
  --rfnorm_nan                                            Positions with read coverage below this threshold are reported as NaN (`--nan`). Auto: 1000 for MaP, 
50 for RT-stop. Set to 0 to disable NaN masking.  
  --rfnorm_prefilter_min_coverage      [integer]          Genome route only: after rf-rctools extract, keep transcripts with at least one position at this 
coverage or higher before rf-norm. Set to 0 to keep the full annotation RC. [default: 1]  
  --rfnorm_img                         [boolean]          Deprecated: rf-norm plot generation (`--img`) is always enabled. [default: false] 
  --rffold_img                         [boolean]          Generate ViennaRNA RNAplot structure diagrams via rf-fold (`-g`). Disabled by default; slow on large 
transcriptomes. Use `--r2dt` for template-based diagrams instead. [default: false]  
  --jackknife_reference                [string]           Path to a reference `.db` structure file for rf-jackknife normalisation assessment. When provided, 
rf-jackknife runs between rf-norm and rf-fold and the output CSV reports optimal slope/intercept 
values. When omitted, rf-fold runs directly using whatever slope/intercept params are set.  
  --rfjackknife_slope                  [string]           Comma-separated slope range to search in rf-jackknife (`-sl`), e.g. `0,5`. [default: 0,5] 
  --rfjackknife_intercept              [string]           Comma-separated intercept range to search in rf-jackknife (`-in`), e.g. `-3,0`. [default: -3,0] 
 
  --rfjackknife_slope_step             [number]           Step size for slope grid search in rf-jackknife (`-ss`). [default: 0.2] 
  --rfjackknife_intercept_step         [number]           Step size for intercept grid search in rf-jackknife (`-is`). [default: 0.2] 
  --rfjackknife_mfmi                   [boolean]          Use modified FMI (Lan et al. 2022) instead of standard FMI in rf-jackknife (`-m`). [default: 
true]  
  --rfjackknife_relaxed                [boolean]          Use relaxed FMI criteria (Deigan et al. 2009) in rf-jackknife (`-x`). [default: true] 
  --rfjackknife_keep_lonelypairs       [boolean]          Retain lonely base-pairs (1 bp helices) in the reference structure before comparison (`-kl`). 
[default: true]  
  --rfjackknife_keep_pseudoknots       [boolean]          Retain pseudoknotted base-pairs in the reference structure during FMI comparison (`-kp`). Disabled 
by default because rf-fold cannot predict pseudoknots, so including them systematically lowers FMI. 
 
  --rfjackknife_only_common            [boolean]          Only use transcripts present across all replicates in rf-jackknife (`-oc`). 
  --rfjackknife_img                    [boolean]          Generate R heatmap of grid-search results in rf-jackknife (`-g`). 
  --rfjackknife_rf_fold_params         [string]           Additional rf-fold parameters passed inside rf-jackknife (`-rp`), e.g. `-md 500`. [default: -md 
600]  
  --rfjackknife_pool_all               [boolean]          Pool XMLs from all sample groups into a single rf-jackknife run (output: `jackknife/all_groups/`) 
instead of running one jackknife per group. Default: true. [default: true]  
  --stop_after_jackknife               [boolean]          Stop the pipeline after rf-jackknife completes, skipping rf-fold, structure visualisation, browser 
track generation, and RDAT output. Useful for calibration runs where you only want jackknife 
statistics. [default: false]  
  --rfrctools_gtf_feature              [string]           GTF feature type to extract with rf-rctools (`-f`); override for non-standard annotations. 
[default: exon]  
  --rfrctools_gtf_attribute            [string]           GTF attribute used as the output RC entry ID by rf-rctools (`-b`). [default: transcript_id] 
  --rfeval_reference                   [string]           Path to a `.db` file of known RNA secondary structures. Enables rf-eval when provided. 
  --rfeval_windows                     [string]           Optional manifest of sub-region references, one per line: `ref_seq_id start end structure_id 
[strand]` (1-based inclusive, in the XML's own coordinate space). When set, rf-norm reactivities are 
sliced to each window before rf-eval, so a sub-region structure (e.g. an Rfam element on a whole 
chromosome) is scored against a matching windowed XML instead of the diluted full transcript. 
`structure_id` must match an entry in `--rfeval_reference`.  
  --rfeval_reactivity_cutoff           [number]           Cutoff for classifying a base as highly-reactive in rf-eval unpaired-coefficient calculation (`-c`). 
[default: 0.7]  
  --rfeval_ignore_terminal             [boolean]          Exclude terminal base-pairs from rf-eval calculations (`-it`). [default: true] 
  --rfeval_terminal_as_unpaired        [boolean]          Treat terminal base-pairs as unpaired in rf-eval (`-tu`). [default: false] 
  --rfeval_keep_pseudoknots            [boolean]          Retain pseudoknotted base-pairs in rf-eval (`-kp`). [default: true] 
  --rfeval_keep_lonelypairs            [boolean]          Retain lonely/isolated base-pairs in rf-eval (`-kl`). [default: true] 
  --rfeval_img                         [boolean]          Generate R metric plots in rf-eval (`-g`). [default: false] 
  --structextract                      [boolean]          Run rf-structextract after rf-fold to extract high-confidence, low-reactivity / low-Shannon 
structural motifs from the folded structures. [default: false]  
  --structextract_win_size             [integer]          rf-structextract window size in nt for median reactivity/Shannon calculations (`-w`). [default: 
50]  
  --structextract_min_transcript_len   [integer]          rf-structextract skips low-reactivity/low-Shannon evaluation for transcripts below this length 
(`-ml`). [default: 500]  
  --structextract_min_value_frac       [number]           rf-structextract windows with less than this fraction of bases covered are set to NaN (`-mv`). 
[default: 0.4]  
  --structextract_min_below_median     [number]           rf-structextract minimum fraction of bases whose Shannon and reactivity are below the transcript 
median (`-mb`). [default: 0.7]  
  --structextract_min_paired_frac      [number]           rf-structextract discards elements with less than this fraction of paired bases (`-mp`). 
[default: 0.45]  
  --structextract_min_motif_len        [integer]          rf-structextract discards structure elements shorter than this (`-mm`). [default: 50] 
  --structextract_max_motif_len        [integer]          rf-structextract discards structure elements longer than this (`-xm`). Unset for no limit. 
  --structextract_max_loop_size        [integer]          rf-structextract discards elements with a loop larger than this (`-xl`). Unset for no limit. 
 
  --structextract_ignore_react         [boolean]          rf-structextract skips low-reactivity evaluation (`-ir`). When false (default), reactivity is 
evaluated so only regions with probing support are extracted. [default: false]  
  --structextract_ignore_shannon       [boolean]          rf-structextract skips low-Shannon evaluation (`-is`). When `false`, the Shannon-entropy test is 
applied so only high-confidence regions are extracted. [default: false]  
  --structextract_multiway_only        [boolean]          rf-structextract only reports elements encompassing multiway junctions (`-mo`). [default: false] 
 
  --structextract_one_per_file         [boolean]          rf-structextract reports each extracted element in a separate file (`-opf`). [default: false] 
 
  --structextract_eval_energy          [boolean]          rf-structextract only reports elements with a free energy significantly lower than expected by 
chance (`-ee`). [default: false]  
  --structextract_pvalue               [number]           rf-structextract p-value threshold for energy significance (`-v`); requires 
`--structextract_eval_energy`. [default: 0.05]  
  --structextract_n_shufflings         [integer]          rf-structextract number of sequence shufflings for energy evaluation (`-ns`). [default: 100] 
 
  --structextract_dinucl_shuffle       [boolean]          rf-structextract preserves dinucleotide frequencies when shuffling for energy evaluation (`-ds`). 
[default: false]  
  --structextract_plot                 [boolean]          Render an SVG diagram per extracted rf-structextract motif via ViennaRNA RNAplot. [default: 
true]  
  --correlate_replicates               [boolean]          Run rf-correlate to compute pairwise reactivity-profile correlations between replicates of each 
sample group (a replicate-reproducibility QC surfaced in MultiQC). Only runs for sample groups with 
more than one replicate; a no-op otherwise. [default: true]  
  --rfcorrelate_cap_react              [number]           Cap reactivities to this value before Pearson correlation in rf-correlate (`--cap-react`). Not 
applied to the Spearman run, which is rank-based. [default: 1.5]  
  --correlate_min_values               [number]           rf-correlate minimum number of values to calculate a correlation (`-m`); a value between 0 and 1 is 
interpreted as a fraction of transcript length. Unset uses the tool default (off).  
  --correlate_ignore_sequence          [boolean]          rf-correlate ignores sequence differences (e.g. SNVs) between compared transcripts (`-I`). 
[default: false]  
  --correlate_img                      [boolean]          Generate the rf-correlate correlation heatmap PDF (`-g`; requires R). [default: false] 
  --rffold_ct                          [boolean]          Write CT format structures with rf-fold (`-ct`). Disabled by default to keep dot-bracket output. 
[default: false]  
  --rffold_window                      [integer]          Enable windowed MFE folding in rf-fold with this window size (`-w -fw N`). Set null/unset to fold 
the whole transcript instead. [default: 1000]  
  --rffold_partition_window            [integer]          Partition-function window size in rf-fold (`-pw N`). [default: 1000] 
  --rffold_unconstrained               [boolean]          Fold without reactivity constraints in rf-fold (`-u`). [default: false] 
  --rffold_vienna_no_lonely_pairs      [boolean]          Pass ViennaRNA no-lonely-pairs mode to rf-fold (`-vnlp`). [default: false] 
  --rffold_vienna_constrained          [boolean]          Use ViennaRNA hard constraints with rf-fold (`-vc`). [default: false] 
  --rffold_vienna_max_bp_span          [integer]          Maximal base-pair span for ViennaRNA in rf-fold (`-vmd`). [default: 600] 
  --rffold_vienna_bp_span              [integer]          Minimal base-pair span for ViennaRNA in rf-fold (`-vms`). 
  --rffold_only_common                 [integer]          Only fold transcripts covered in at least this number of XML experiments (`-oc`). 
  --rffold_fold_constraint_file        [string]           Constraint file for allowed base-pairing positions (`-fc`). 
  --rffold_unpaired_constraint_file    [string]           Constraint file for required unpaired positions (`-uc`). 
  --rffold_dotplot                     [boolean]          Generate dot plots from rf-fold (`-d`). [default: true] 
  --rffold_shannon_entropy             [boolean]          Compute and report Shannon entropy in rf-fold (`-sh`). [default: true] 
  --rffold_slope                       [number]           Slope for reactivity-to-folding-constraint conversion in rf-fold (`-sl`). Auto by `chemical`: DMS 
4.6, NAI 2.2, 2A3 1, other/unset 1.8. Overridden by jackknife calibration when available.  
  --rffold_intercept                   [number]           Intercept for reactivity-to-folding-constraint conversion in rf-fold (`-in`). Auto by `chemical`: 
DMS -2, NAI -0.8, 2A3 -0.4, other/unset -0.6. Overridden by jackknife calibration when available. 
 
  --rffold_vienna_rnaplot              [string]           Path to ViennaRNA `RNAplot` binary for SVG structure plots in rf-fold (`-vrp`). [default: 
RNAplot]  
  --r2dt                               [boolean]          Generate template-based 2D RNA structure diagrams using R2DT, coloured by normalised 
SHAPE/chemical-probing reactivity. [default: true]  
  --r2dt_container                     [string]           Container image used for the local R2DT module. The default tracks the rnacentral/r2dt :latest tag, 
validated at digest sha256:7ce2f54a7a3ff1d7fee7430209a7358ae1e971644446cfe4906dbd7b6211282c 
(2026-06). A tag is used rather than that digest because Wave cannot attach its Fusion 
containerConfig to a @sha256 reference, which fails the run on AWS/Seqera Platform. An earlier build 
shipped the ribovision-ssu/lsu, rnasep and tmrna covariance models without their cmfetch SSI 
indexes; because the data dir is read-only under Singularity, R2DT could not build them at runtime 
and those templates (e.g. LSU rRNA) failed to draw. [default: docker.io/rnacentral/r2dt:latest] 
 
  --r2dt_templatable_biotypes          [string]           Comma-separated GTF biotypes sent to R2DT. R2DT only has templates for structured ncRNA classes; 
transcripts of other biotypes (mRNA, lncRNA, ...) are excluded so R2DT never force-fits them to the 
wrong template, and are drawn by ViennaRNA instead. Matched case-insensitively against 
transcript/gene biotype. [default: 
rRNA,Mt_rRNA,tRNA,Mt_tRNA,snoRNA,scaRNA,snRNA,SRP_RNA,RNase_P_RNA,RNase_MRP_RNA,tmRNA]  
  --rfwiggle_report_zeroes             [boolean]          Report positions with zero reactivity in the WIG/BigWig output (`-z`). Enabled by default so genome 
browsers receive complete tracks without gaps. [default: true]  
  --rfwiggle_keep_bases                [string]           Restrict WIG output to specific bases using an IUPAC code or combination (e.g. `AC` for DMS probing) 
(`-kb`). Default: all bases (`N`).  
  --rnaframework_r_path                [string]           Path to R executable used by rf-count, rf-norm, and rf-fold plotting. [default: /usr/bin/R] 

Executor options
  --slurm_queue                        [string] SLURM partition / queue name passed via `--partition`. 
  --slurm_account                      [string] SLURM account / allocation passed via `--account`. 
  --slurm_clusterOptions               [string] Extra sbatch flags appended verbatim to `process.clusterOptions`. 

Generic options
  --multiqc_methods_description        [string]          Custom MultiQC yaml file containing HTML including a methods description. 
  --help                               [boolean, string] Display the help message. 
  --help_full                          [boolean]         Display the full detailed help message. 
  --show_hidden                        [boolean]         Display hidden parameters in the help message (only works when --help or --help_full are provided). 
 
 !! Hiding 21 param(s), use the `--showHidden` parameter to show them !!
------------------------------------------------------

* The nf-core framework
    https://doi.org/10.1038/s41587-020-0439-x

* Software dependencies
    https://github.com/nf-core/rnastructurome/blob/master/CITATIONS.md

